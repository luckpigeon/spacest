version: '3'
services:
  heimdall:
    image: linuxserver/heimdall:latest
    ports: [8080:80]
    volumes: [configdata:/config]
volumes:
  configdata:
    driver: rclone
    driver_opts:
      remote: 'e5:heimdall'
      allow_other: 'true'
      vfs_cache_mode: writes
      poll_interval: 0